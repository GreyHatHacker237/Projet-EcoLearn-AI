spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ecolearn
    username: postgres
    password: ${POSTGRES_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
